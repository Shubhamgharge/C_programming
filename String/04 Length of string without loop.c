///length of string without loop

#include<stdio.h>
#include<conio.h>

int main()
{

    char sname[20]={'\0'};

    printf("Enter a string : ");
    gets(sname);

    printf("length: %d    ", printf(sname));

    getch();
    return 0;
}
